# Miracle Mile Shops — miraclemileshopslv.com

WordPress site on WP Engine (install `miraclemilesh`, account access@classiccityconsulting.com).
Beaver Builder parent theme 1.7.20 + child theme; site content is built in Beaver Builder.

| Path | What |
|---|---|
| `themes/bb-theme/` | Beaver Builder parent theme as downloaded 2026-09-17 (injection already removed) |
| `themes/bb-theme-child/` | Child theme (ACF read-only field filters, Gravity Forms coupon counter) |
| `plugins/` | Reserved for custom plugin code (empty; all plugins on the site are third-party) |
| `incident-2026-09-17/` | Security incident record: report, timeline, change log, evidence |
| `BUILD_LOG.md` | Milestone log for work on this repo |

SSH: `ssh miraclemilesh@miraclemilesh.ssh.wpengine.net` (key "Laptop for Access", configured in `~/.ssh/config`).

Malware samples under `incident-2026-09-17/evidence/` are stored with a `.txt` extension and are inert.
